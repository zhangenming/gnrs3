用中文回答
每次prompt过后 提交一次git, commit为prompt

<br />

所有的代码应该在一个js文件内
变量名, 函数名,参数 等尽量使用中文名
如果一个函数只在一个函数下使用 把这个函数的定义 放到这个函数内




```javascript
        observer.observe(document.body, { 
            zem: true // 都要添加这个参数
        });

```

术语:
基地
塔 有人塔 我方塔 敌方塔



业务相关:
一大回合有25小回合 一小回合有2turn
每turn可以走一步

每回合(2turn) 基地和塔的兵力会+1
每大回合(50turn) 所有的位置的兵力都会+1 (塔和基地会+2)