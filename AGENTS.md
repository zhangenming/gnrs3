chrome版本是143 只在这个浏览器下执行 不需要考虑兼容性 使用最新的语法
所有的代码应该在一个js文件内
变量名, 函数名,参数 等尽量使用中文名; 但这种"索引"不需要用中文 直接用idx可读性更高. 简而言之 用中文是为了可读性
如果一个函数只在一个函数下使用 把这个函数的定义 放到这个函数内最底部
const fn = ()=>{} 使用function fn的形式替代

```javascript
observer.observe(document.body, {
  zem: true, // 都要添加这个参数
});
```

<!--  -->

术语:
基地
塔 有人塔 我方塔 敌方塔

业务相关:
一大回合有25小回合 一小回合有2turn
每turn可以走一步

每回合(2turn) 基地和塔的兵力会+1
每大回合(50turn) 所有的位置的兵力都会+1 (塔和基地会+2)

<!--  -->
<!--  -->
<!--  -->

用中文回答
每次prompt过后 提交一次git, commit为prompt; 提交前执行prettier

<!--  -->

1. 编码前思考
   不要假设。不要隐藏困惑。呈现权衡。
   明确说明假设 — 如果不确定，询问而不是猜测
   呈现多种解释 — 当存在歧义时，不要默默选择
   适时提出异议 — 如果存在更简单的方法，说出来
   困惑时停下来 — 指出不清楚的地方并要求澄清

2. 简洁优先
   用最少的代码解决问题。不要过度推测。

对抗过度工程的倾向：

不要添加要求之外的功能
不要为一次性代码创建抽象
不要添加未要求的"灵活性"或"可配置性"
不要为不可能发生的场景做错误处理
如果 200 行代码可以写成 50 行，重写它
检验标准： 资深工程师会觉得这过于复杂吗？如果是，简化。

3. 精准修改
   只碰必须碰的。只清理自己造成的混乱。

编辑现有代码时：

不要"改进"相邻的代码、注释或格式
不要重构没坏的东西
匹配现有风格，即使你更倾向于不同的写法
当你的改动产生孤儿代码时：

删除因你的改动而变得无用的导入/变量/函数
检验标准： 每一行修改都应该能直接追溯到用户的请求。
